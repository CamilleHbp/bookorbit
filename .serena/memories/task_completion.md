Use smallest relevant test first. Server: pnpm --filter server test <test-path>. Client: pnpm --filter client test:unit --run <test-path>. These scripts use a shared test lock; avoid launching competing full suites. pnpm typecheck runs both workspace checks. pnpm lint:check includes frontend locale/style validation. Before committing run applicable Prettier and ESLint; do not format unrelated changes. Verify request DTO, frontend payload and response together.
