Follow AGENTS.md. Never add Co-authored-by trailers or em dashes. Generate migrations with Drizzle Kit; do not author migration SQL manually. Vue event handlers are bare references except necessary v-for item arguments. Service logs use [event] [start|end|fail]; quoted dynamic fields use sanitizeLogValue. User scope and backend permission guards apply independently of frontend visibility. Expose cross-module behavior through exported services.
